const html = String.raw`<!doctype html>
<html lang="en"><head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<title>ProjectMind AI V3 · PMO Operating System</title>
<script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js"></script>
<style>
:root{--bg:#07111f;--panel:#0e1b2d;--panel2:#11223a;--line:#22344d;--text:#eef4fb;--muted:#93a4b8;--blue:#4f8cff;--green:#35c98b;--amber:#ffbd45;--red:#ff647c;--purple:#9b7cff}
*{box-sizing:border-box}body{margin:0;background:linear-gradient(180deg,#07111f,#0b1626);color:var(--text);font-family:Inter,system-ui,-apple-system,Segoe UI,sans-serif}.app{max-width:1320px;margin:auto;padding:20px}
.top{display:flex;justify-content:space-between;align-items:center;gap:12px}.brand{display:flex;gap:12px;align-items:center}.logo{width:46px;height:46px;border-radius:15px;background:linear-gradient(135deg,#4f8cff,#8a5cff);display:grid;place-items:center;font-size:23px}.brand h1{font-size:22px;margin:0}.brand p{margin:3px 0 0;color:var(--muted);font-size:13px}.online{font-size:13px;color:var(--green);background:#0e2b25;padding:8px 12px;border-radius:99px}
.hero{margin:20px 0;padding:22px;border:1px solid var(--line);border-radius:22px;background:rgba(14,27,45,.85)}.hero h2{margin:0 0 7px;font-size:26px}.hero p{margin:0;color:var(--muted);line-height:1.5}
.grid{display:grid;grid-template-columns:repeat(5,1fr);gap:12px}.card{padding:17px;border:1px solid var(--line);background:var(--panel);border-radius:18px}.card .n{font-size:29px;font-weight:800;margin:10px 0 3px}.label{color:var(--muted);font-size:12px;letter-spacing:.03em}.red{color:var(--red)}.amber{color:var(--amber)}.green{color:var(--green)}.blue{color:var(--blue)}.purple{color:var(--purple)}
.workspace{display:grid;grid-template-columns:1.1fr .9fr;gap:14px;margin-top:14px}.box{border:1px solid var(--line);background:var(--panel);border-radius:20px;padding:18px}.box h3{margin:0 0 14px;font-size:17px}.sub{margin:-6px 0 14px;color:var(--muted);font-size:13px}
.chat{height:270px;overflow:auto;padding:4px 0}.msg{max-width:90%;padding:12px 14px;border-radius:15px;margin:8px 0;line-height:1.5;font-size:14px;white-space:pre-wrap}.bot{background:#16263c}.user{background:#285ea8;margin-left:auto}.compose{display:flex;gap:8px;margin-top:12px}.compose input{flex:1;border:1px solid var(--line);background:#091523;color:white;border-radius:14px;padding:14px;font-size:15px}.compose button,.btn{border:0;border-radius:14px;background:var(--blue);color:#fff;padding:11px 16px;font-weight:700;cursor:pointer}.btn.alt{background:#172b47;border:1px solid var(--line)}.btn.warn{background:#6a4a13}.btn.danger{background:#56202b}.btn:disabled{opacity:.6;cursor:wait}
.focus{padding:0;margin:0}.focus li{list-style:none;padding:13px 0;border-bottom:1px solid var(--line);font-size:14px;line-height:1.45}.focus li:last-child{border:0}.tag{display:inline-block;font-size:11px;padding:4px 8px;border-radius:99px;margin-right:7px}.tred{background:#351923;color:#ff9aaa}.tamb{background:#382b15;color:#ffd27c}.tblue{background:#172d4e;color:#8fb8ff}.tpurple{background:#2b2049;color:#c3b2ff}
.upload{margin-top:14px}.drop{border:1.5px dashed #3b5b83;border-radius:18px;padding:20px;text-align:center;background:#0a1728}.drop strong{display:block;margin-bottom:6px}.drop p{color:var(--muted);margin:6px 0 14px;font-size:13px}.file{display:none}.uploadrow{display:flex;gap:10px;align-items:center;flex-wrap:wrap}.filename{color:var(--muted);font-size:13px}
.analysis{margin-top:14px}.toolbar{display:flex;justify-content:space-between;gap:10px;align-items:center;margin-bottom:12px;flex-wrap:wrap}.toolbar p{margin:0;color:var(--muted);font-size:13px}.tablewrap{overflow:auto;border:1px solid var(--line);border-radius:14px;max-height:440px}table{width:100%;border-collapse:collapse;font-size:13px;min-width:900px}th,td{padding:11px 12px;border-bottom:1px solid var(--line);text-align:left;vertical-align:top}th{position:sticky;top:0;background:#13243b;color:#b9c9dc}.status{padding:4px 8px;border-radius:99px;font-size:11px;font-weight:700}.s-overdue{background:#351923;color:#ff9aaa}.s-risk{background:#382b15;color:#ffd27c}.s-track{background:#0e2b25;color:#77e2b6}.empty{color:var(--muted);padding:26px;text-align:center}
.report{margin-top:14px;background:#091523;border:1px solid var(--line);border-radius:16px;padding:16px;white-space:pre-wrap;line-height:1.55;font-size:14px;min-height:120px}.hidden{display:none}
.v3grid{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:14px}.timeline{display:flex;flex-direction:column;gap:9px;max-height:360px;overflow:auto}.snap{padding:12px;border:1px solid var(--line);background:#091523;border-radius:14px;display:flex;justify-content:space-between;gap:10px;align-items:center}.snap b{font-size:13px}.snap small{display:block;color:var(--muted);margin-top:3px}.snap .actions{display:flex;gap:6px}.mini{padding:7px 10px;border-radius:10px;border:1px solid var(--line);background:#172b47;color:#fff;cursor:pointer;font-size:12px}
.alert{padding:12px;border:1px solid var(--line);border-radius:14px;margin-bottom:8px;background:#091523;font-size:13px;line-height:1.45}.alert b{display:block;margin-bottom:4px}.a-red{border-left:3px solid var(--red)}.a-amber{border-left:3px solid var(--amber)}.a-blue{border-left:3px solid var(--blue)}
.change{font-size:11px;padding:4px 8px;border-radius:99px;display:inline-block}.c-new{background:#0e2b25;color:#77e2b6}.c-change{background:#382b15;color:#ffd27c}.c-escalate{background:#351923;color:#ff9aaa}.c-none{background:#172b47;color:#8fb8ff}
.footerNote{margin-top:12px;color:var(--muted);font-size:12px}
@media(max-width:980px){.grid{grid-template-columns:repeat(3,1fr)}}@media(max-width:760px){.app{padding:14px}.grid{grid-template-columns:repeat(2,1fr)}.workspace,.v3grid{grid-template-columns:1fr}.hero h2{font-size:22px}.top{align-items:flex-start}.online{white-space:nowrap}.chat{height:230px}.drop{padding:16px}.compose button{padding:0 14px}.card .n{font-size:26px}.snap{align-items:flex-start;flex-direction:column}}
</style></head><body><main class="app">
<header class="top"><div class="brand"><div class="logo">🧠</div><div><h1>ProjectMind AI <span class="label">V3</span></h1><p>PMO Operating System · Delivery Intelligence · Recovery Governance</p></div></div><div class="online" id="brain">● Checking AI Brain</div></header>

<section class="hero"><h2>From tracker to project control.</h2><p>V3 keeps project snapshots in your browser, detects what changed between reporting cycles, highlights deteriorating commitments and prepares an executive-ready PMO recovery view.</p></section>

<section class="grid">
<div class="card"><div class="label">OVERDUE</div><div class="n red" id="overdue">0</div><div class="label">Recovery action required</div></div>
<div class="card"><div class="label">AT RISK</div><div class="n amber" id="risk">0</div><div class="label">Monitor closely</div></div>
<div class="card"><div class="label">COMPLETED</div><div class="n green" id="complete">0%</div><div class="label">Overall completion</div></div>
<div class="card"><div class="label">PMO FOCUS</div><div class="n blue" id="focusN">0</div><div class="label">Escalation items</div></div>
<div class="card"><div class="label">CHANGED</div><div class="n purple" id="changedN">0</div><div class="label">Since last snapshot</div></div>
</section>

<section class="workspace">
<div class="box"><h3>🤖 Ask ProjectMind AI</h3><div class="chat" id="chat"><div class="msg bot">I’m ready. Upload a tracker and I can help analyse delayed tasks, identify what changed since the previous snapshot, build a recovery plan, prepare escalation points or write an executive PMO update.</div></div><div class="compose"><input id="q" placeholder="Example: What deteriorated since the last snapshot?" autocomplete="off"><button id="send">Ask</button></div></div>
<div class="box"><h3>🎯 Need Attention</h3><ul class="focus" id="focusList"><li><span class="tag tblue">PMO</span>Upload a project tracker to start delivery intelligence.</li></ul></div>
</section>

<section class="box upload"><h3>📥 Project Tracker → V3 Control Cycle</h3>
<div class="drop"><strong>Upload Excel or CSV Project Tracker</strong><p>Supported: .xlsx, .xls, .csv · ProjectMind detects Task, Status, PIC/Owner, ETA, Due Date, Stream and Dependency. The tracker stays in your browser unless you explicitly request AI analysis.</p><div class="uploadrow"><label class="btn" for="file">Choose File</label><input class="file" id="file" type="file" accept=".xlsx,.xls,.csv"><span class="filename" id="filename">No file selected</span><button class="btn alt" id="sample">Load Demo Data</button><button class="btn warn" id="snapshotBtn">Save Snapshot</button></div></div>
<div class="footerNote">Snapshot history is stored locally in this browser so you can compare daily/weekly tracker versions without adding a database binding.</div>
</section>

<section class="box analysis hidden" id="analysis">
<div class="toolbar"><div><h3>🧭 Recovery Plan Workspace</h3><p id="summaryLine"></p></div><div class="uploadrow"><button class="btn" id="analyseBtn">Generate AI Recovery Plan</button><button class="btn alt" id="exportBtn">Export CSV</button></div></div>
<div class="tablewrap"><table><thead><tr><th>Task</th><th>Status</th><th>Change</th><th>PIC / Owner</th><th>Commit Date</th><th>Impact Analysis</th><th>Next Action / Recovery</th></tr></thead><tbody id="rows"></tbody></table></div>
<div class="report" id="report">Upload a tracker to generate the PMO recovery view.</div>
</section>

<section class="v3grid">
<div class="box"><div class="toolbar"><div><h3>🕘 Historical Snapshots</h3><p class="sub">Use snapshots as reporting-cycle baselines for change detection.</p></div><button class="btn danger" id="clearHistory">Clear History</button></div><div class="timeline" id="timeline"><div class="empty">No snapshots saved yet.</div></div></div>
<div class="box"><div class="toolbar"><div><h3>🚨 PMO Alert Centre</h3><p class="sub">Automatically generated from current risk and tracker changes.</p></div><button class="btn alt" id="briefBtn">Generate Daily Brief</button></div><div id="alerts"><div class="empty">Alerts will appear after a tracker is loaded.</div></div></div>
</section>

<section class="box analysis hidden" id="briefBox"><div class="toolbar"><div><h3>📊 Executive Daily Brief</h3><p class="sub">Management-ready summary based on the latest tracker and previous snapshot.</p></div><button class="btn alt" id="copyBrief">Copy Brief</button></div><div class="report" id="brief"></div></section>
</main>

<script>
const STORAGE='projectmind_v3_snapshots';
const chat=document.getElementById('chat'),q=document.getElementById('q'),send=document.getElementById('send'),brain=document.getElementById('brain');
const file=document.getElementById('file'),filename=document.getElementById('filename'),analysis=document.getElementById('analysis'),rowsEl=document.getElementById('rows'),report=document.getElementById('report');
let tasks=[],recovery=[],changes=[];

function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function add(text,kind){const d=document.createElement('div');d.className='msg '+kind;d.textContent=text;chat.appendChild(d);chat.scrollTop=chat.scrollHeight}
async function ask(){const text=q.value.trim();if(!text)return;q.value='';add(text,'user');send.disabled=true;send.textContent='...';try{const r=await fetch('/api/chat',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({message:text,projectContext:tasks.slice(0,100),changeContext:changes.slice(0,80)})});const j=await r.json();add(j.reply||j.error||'No response received.','bot')}catch(e){add('Connection error. Please try again.','bot')}finally{send.disabled=false;send.textContent='Ask'}}
send.onclick=ask;q.addEventListener('keydown',e=>{if(e.key==='Enter')ask()});

function norm(v){return String(v??'').trim()}
function find(obj,names){const keys=Object.keys(obj);for(const n of names){const k=keys.find(x=>x.toLowerCase().replace(/[^a-z0-9]/g,'').includes(n));if(k&&norm(obj[k]))return norm(obj[k])}return ''}
function toDate(v){if(!v)return null;if(typeof v==='number'&&window.XLSX){const p=XLSX.SSF.parse_date_code(v);if(p)return new Date(p.y,p.m-1,p.d)}const d=new Date(v);return isNaN(d)?null:d}
function classify(t){const s=t.status.toLowerCase(),due=toDate(t.commitDate)||toDate(t.dueDate),today=new Date();today.setHours(0,0,0,0);if(/complete|closed|done|cancel/.test(s))return 'track';if(/delay|overdue|late|blocked/.test(s)||(due&&due<today))return 'overdue';if(/risk|issue|pending|hold/.test(s)||(due&&((due-today)/86400000)<=7))return 'risk';return 'track'}
function enrich(t){const c=classify(t);let impact='Delivery timeline may be affected if the activity is not recovered within the committed window.';let action='Confirm root cause, dependency and owner; agree a recovery action with a dated checkpoint.';if(c==='overdue'){impact='High risk to the downstream milestone and dependent activities. Repeated slippage may require formal escalation and recovery planning.';action='Obtain a firm root cause, corrective action, accountable PIC and revised commitment date. Track daily until closure.'}if(c==='risk'){impact='May impact the next milestone if the dependency or pending decision is not resolved on time.';action='Validate dependency, remove blocker and set a short control checkpoint before the committed date.'}return {...t,class:c,impact,action,key:(t.task+'|'+t.stream).toLowerCase().replace(/\\s+/g,' ')}}
function parseRows(raw){tasks=raw.map(x=>({task:find(x,['task','activity','item','description','workitem','taskname'])||'Unnamed task',status:find(x,['status','progress','state'])||'Open',owner:find(x,['pic','owner','assignee','responsible','lead'])||'TBC',commitDate:find(x,['commitdate','eta','targetdate','duedate','enddate','finishdate']),dueDate:find(x,['duedate','targetdate','enddate','finishdate']),stream:find(x,['stream','workstream','module','project']),dependency:find(x,['dependency','blocker','dependencies'])})).filter(x=>x.task!=='Unnamed task').map(enrich);changes=compareWithLatest();recovery=tasks.filter(x=>x.class!=='track');render()}
function loadHistory(){try{return JSON.parse(localStorage.getItem(STORAGE)||'[]')}catch{return []}}
function saveHistory(h){localStorage.setItem(STORAGE,JSON.stringify(h.slice(0,30)))}
function snapshotNow(){if(!tasks.length){alert('Load a tracker first.');return}const h=loadHistory();const snap={id:Date.now(),createdAt:new Date().toISOString(),filename:filename.textContent||'Project Tracker',tasks:tasks.map(t=>({task:t.task,status:t.status,owner:t.owner,commitDate:t.commitDate,dueDate:t.dueDate,stream:t.stream,dependency:t.dependency,class:t.class,key:t.key}))};h.unshift(snap);saveHistory(h);renderHistory();changes=compareWithLatest(1);render();alert('Snapshot saved. Future uploads will be compared against this baseline.')}
function compareWithLatest(skip=0){const h=loadHistory();const prev=h[skip];if(!prev||!Array.isArray(prev.tasks))return [];const old=new Map(prev.tasks.map(t=>[t.key||((t.task+'|'+(t.stream||'')).toLowerCase()),t]));const now=new Map(tasks.map(t=>[t.key,t]));const out=[];for(const [k,t] of now){const p=old.get(k);if(!p){out.push({type:'new',task:t.task,detail:'New task appeared in the current tracker.',severity:'blue',current:t})}else{const dif=[];if(norm(p.status)!==norm(t.status))dif.push('Status: '+p.status+' → '+t.status);if(norm(p.owner)!==norm(t.owner))dif.push('PIC: '+p.owner+' → '+t.owner);if(norm(p.commitDate)!==norm(t.commitDate))dif.push('Commit date: '+(p.commitDate||'TBC')+' → '+(t.commitDate||'TBC'));const rank={track:0,risk:1,overdue:2};if((rank[t.class]??0)>(rank[p.class]??0))dif.push('Risk level deteriorated');if((rank[t.class]??0)<(rank[p.class]??0))dif.push('Risk level improved');if(dif.length){const esc=(rank[t.class]??0)>(rank[p.class]??0);out.push({type:esc?'escalate':'change',task:t.task,detail:dif.join(' · '),severity:esc?'red':'amber',current:t,previous:p})}}}for(const [k,p] of old)if(!now.has(k))out.push({type:'removed',task:p.task,detail:'Task is no longer present in the current tracker.',severity:'blue',previous:p});return out}
function changeLabel(c){if(c.type==='new')return '<span class="change c-new">NEW</span>';if(c.type==='escalate')return '<span class="change c-escalate">DETERIORATED</span>';if(c.type==='change')return '<span class="change c-change">CHANGED</span>';return '<span class="change c-none">REMOVED</span>'}
function render(){analysis.classList.remove('hidden');const o=tasks.filter(x=>x.class==='overdue').length,r=tasks.filter(x=>x.class==='risk').length,c=tasks.filter(x=>/complete|closed|done/.test(x.status.toLowerCase())).length;document.getElementById('overdue').textContent=o;document.getElementById('risk').textContent=r;document.getElementById('complete').textContent=tasks.length?Math.round(c/tasks.length*100)+'%':'0%';document.getElementById('focusN').textContent=Math.min(3,o+r);document.getElementById('changedN').textContent=changes.length;document.getElementById('summaryLine').textContent=tasks.length+' tasks loaded · '+o+' overdue · '+r+' at risk · '+c+' completed · '+changes.length+' changes vs baseline';
const cmap=new Map(changes.filter(x=>x.current).map(x=>[x.current.key,x]));rowsEl.innerHTML='';(recovery.length?recovery:tasks).slice(0,250).forEach(t=>{const tr=document.createElement('tr'),tag=t.class==='overdue'?'s-overdue':t.class==='risk'?'s-risk':'s-track',ch=cmap.get(t.key);tr.innerHTML='<td>'+esc(t.task)+(t.stream?'<br><span class="label">'+esc(t.stream)+'</span>':'')+'</td><td><span class="status '+tag+'">'+esc(t.status)+'</span></td><td>'+(ch?changeLabel(ch)+'<br><span class="label">'+esc(ch.detail)+'</span>':'<span class="change c-none">NO CHANGE</span>')+'</td><td>'+esc(t.owner)+'</td><td>'+esc(t.commitDate||'TBC')+'</td><td>'+esc(t.impact)+'</td><td>'+esc(t.action)+(t.dependency?'<br><span class="label">Dependency: '+esc(t.dependency)+'</span>':'')+'</td>';rowsEl.appendChild(tr)});renderFocus(o,r);renderAlerts(o,r);report.textContent=localReport();renderHistory()}
function renderFocus(o,r){const deteriorated=changes.filter(x=>x.type==='escalate').length;const noEta=recovery.filter(x=>!norm(x.commitDate)||/tbc/i.test(x.commitDate)).length;document.getElementById('focusList').innerHTML=['<li><span class="tag tred">OVERDUE</span>'+o+' task(s) require owner commitment and dated recovery action.</li>','<li><span class="tag tamb">AT RISK</span>'+r+' task(s) require dependency and blocker validation.</li>','<li><span class="tag tpurple">CHANGE</span>'+deteriorated+' task(s) deteriorated since the last snapshot.</li>','<li><span class="tag tblue">ETA</span>'+noEta+' recovery item(s) still have no usable commitment date.</li>'].join('')}
function renderAlerts(o,r){const el=document.getElementById('alerts'),a=[];if(o)a.push({cl:'a-red',title:'Immediate recovery control',text:o+' overdue task(s) are affecting delivery confidence. Confirm root cause, accountable PIC and revised commitment before the next governance checkpoint.'});const bad=changes.filter(x=>x.type==='escalate');if(bad.length)a.push({cl:'a-red',title:'Deteriorating commitments',text:bad.length+' task(s) worsened compared with the previous snapshot. These should be reviewed before they become normalised delays.'});const eta=recovery.filter(x=>!norm(x.commitDate)||/tbc/i.test(x.commitDate));if(eta.length)a.push({cl:'a-amber',title:'Commitment governance gap',text:eta.length+' recovery item(s) have no usable ETA. Tracker follow-up should close this gap immediately.'});if(r)a.push({cl:'a-amber',title:'Dependency control',text:r+' at-risk task(s) require blocker/dependency validation before the next milestone or test cycle.'});if(changes.length)a.push({cl:'a-blue',title:'Reporting-cycle change detected',text:changes.length+' change(s) were detected against the latest saved baseline. Use the Executive Daily Brief to communicate movement to management.'});el.innerHTML=a.length?a.map(x=>'<div class="alert '+x.cl+'"><b>'+esc(x.title)+'</b>'+esc(x.text)+'</div>').join(''):'<div class="empty">No immediate PMO alerts generated from the current tracker.</div>'}
function localReport(){const o=tasks.filter(x=>x.class==='overdue'),r=tasks.filter(x=>x.class==='risk'),bad=changes.filter(x=>x.type==='escalate'),names=o.slice(0,5).map(x=>'• '+x.task+' — '+x.owner+' — Commit: '+(x.commitDate||'TBC')).join('\\n')||'• No overdue tasks detected.';return 'PMO RECOVERY SUMMARY\\n\\nCurrent position: '+o.length+' overdue and '+r.length+' at-risk task(s) identified. '+changes.length+' tracker change(s) detected versus the latest saved snapshot, including '+bad.length+' deterioration(s).\\n\\nImmediate action:\\n1. Confirm root cause and accountable PIC for every overdue item.\\n2. Validate dependencies and remove blockers before the next control checkpoint.\\n3. Obtain a realistic committed date; do not leave ETA as TBC.\\n4. Escalate repeated missed commitments where the next milestone is impacted.\\n5. Save a new snapshot after the next reporting cycle to measure whether recovery actions are working.\\n\\nPriority items:\\n'+names}
function renderHistory(){const h=loadHistory(),el=document.getElementById('timeline');if(!h.length){el.innerHTML='<div class="empty">No snapshots saved yet.</div>';return}el.innerHTML=h.map((s,i)=>'<div class="snap"><div><b>'+esc(s.filename||'Project Tracker')+'</b><small>'+new Date(s.createdAt).toLocaleString()+' · '+(s.tasks||[]).length+' tasks</small></div><div class="actions">'+(i===0?'<span class="change c-new">LATEST BASELINE</span>':'')+'<button class="mini" onclick="deleteSnapshot('+s.id+')">Delete</button></div></div>').join('')}
window.deleteSnapshot=id=>{saveHistory(loadHistory().filter(s=>s.id!==id));changes=compareWithLatest();if(tasks.length)render();else renderHistory()}
function generateBrief(){if(!tasks.length){alert('Load a tracker first.');return}const o=tasks.filter(x=>x.class==='overdue').length,r=tasks.filter(x=>x.class==='risk').length,c=tasks.filter(x=>/complete|closed|done/.test(x.status.toLowerCase())).length,bad=changes.filter(x=>x.type==='escalate'),changed=changes.filter(x=>x.type!=='removed').slice(0,5);const text='PROJECTMIND AI V3 – EXECUTIVE DAILY BRIEF\\n\\nOverall position\\n• Total tasks: '+tasks.length+'\\n• Overdue: '+o+'\\n• At Risk: '+r+'\\n• Completed: '+(tasks.length?Math.round(c/tasks.length*100):0)+'%\\n• Changes since last snapshot: '+changes.length+'\\n• Deteriorated items: '+bad.length+'\\n\\nManagement attention\\n'+(o?'• '+o+' overdue item(s) require a firm recovery commitment from the accountable PIC.\\n':'• No overdue item currently detected.\\n')+(r?'• '+r+' at-risk item(s) require dependency/blocker validation before the next control point.\\n':'')+(bad.length?'• Deterioration detected: '+bad.slice(0,3).map(x=>x.task).join('; ')+'.\\n':'')+'\\nKey movement\\n'+(changed.length?changed.map(x=>'• '+x.task+': '+x.detail).join('\\n'):'• No material tracker movement detected against the latest baseline.')+'\\n\\nRequired PMO action today\\n1. Close all open ownership and ETA gaps for overdue items.\\n2. Validate dependencies for at-risk activities.\\n3. Escalate repeated missed commitments with quantified milestone impact.\\n4. Reflect agreed recovery actions and final solutions in the relevant project documents.\\n5. Save the next tracker snapshot after the reporting cycle to measure recovery progress.';document.getElementById('briefBox').classList.remove('hidden');document.getElementById('brief').textContent=text}
file.onchange=async()=>{const f=file.files[0];if(!f)return;filename.textContent=f.name;try{const data=await f.arrayBuffer();const wb=XLSX.read(data,{type:'array',cellDates:true});const ws=wb.Sheets[wb.SheetNames[0]];parseRows(XLSX.utils.sheet_to_json(ws,{defval:''}))}catch(e){report.textContent='Unable to read this file. Please use a standard Excel or CSV tracker.';analysis.classList.remove('hidden')}}
document.getElementById('sample').onclick=()=>{filename.textContent='ProjectMind demo tracker';parseRows([{Task:'SIT Defect Resolution',Status:'Delayed',PIC:'Partner A',ETA:'2026-09-05',Stream:'SIT',Dependency:'Environment stability'},{Task:'DHL SFTP Folder Configuration',Status:'At Risk',PIC:'Integration Team',ETA:'2026-09-07',Stream:'Integration',Dependency:'DHL confirmation'},{Task:'PB2 Solution Document Update',Status:'Open',PIC:'Business Analyst',ETA:'2026-09-12',Stream:'Solution',Dependency:'Open concerns closure'},{Task:'UAT Entry Criteria',Status:'Completed',PIC:'PMO',ETA:'2026-09-01',Stream:'Governance'}])}
document.getElementById('snapshotBtn').onclick=snapshotNow;document.getElementById('clearHistory').onclick=()=>{if(confirm('Clear all local ProjectMind snapshot history?')){localStorage.removeItem(STORAGE);changes=compareWithLatest();if(tasks.length)render();else renderHistory()}};document.getElementById('briefBtn').onclick=generateBrief;document.getElementById('copyBrief').onclick=async()=>{try{await navigator.clipboard.writeText(document.getElementById('brief').textContent);alert('Brief copied.')}catch{alert('Copy is not available in this browser.')}}
document.getElementById('analyseBtn').onclick=async()=>{if(!tasks.length)return;const b=document.getElementById('analyseBtn');b.disabled=true;b.textContent='Analysing...';report.textContent='ProjectMind is preparing the recovery analysis...';try{const res=await fetch('/api/analyze',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({tasks:recovery.slice(0,120),changes:changes.slice(0,80)})});const j=await res.json();report.textContent=j.report||localReport()}catch(e){report.textContent=localReport()}finally{b.disabled=false;b.textContent='Generate AI Recovery Plan'}}
document.getElementById('exportBtn').onclick=()=>{const h=['Task','Status','PIC/Owner','Commit Date','Change','Impact Analysis','Next Action','Dependency'];const cmap=new Map(changes.filter(x=>x.current).map(x=>[x.current.key,x]));const lines=[h,...(recovery.length?recovery:tasks).map(t=>[t.task,t.status,t.owner,t.commitDate,cmap.get(t.key)?.detail||'',t.impact,t.action,t.dependency])].map(r=>r.map(v=>'"'+String(v??'').replace(/"/g,'""')+'"').join(',')).join('\\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([lines],{type:'text/csv'}));a.download='ProjectMind_V3_Recovery_and_Changes.csv';a.click();URL.revokeObjectURL(a.href)}
fetch('/api/health').then(r=>r.json()).then(j=>{brain.textContent=j.aiConfigured?'● AI Brain Online · V3':'● Dashboard Online · Local intelligence active';brain.style.color=j.aiConfigured?'var(--green)':'var(--amber)'}).catch(()=>{brain.textContent='● Connection unavailable';brain.style.color='var(--red)'});
</script></body></html>`;

const jsonHeaders={"content-type":"application/json; charset=UTF-8"};
function cleanTask(t){return {task:String(t.task||''),status:String(t.status||''),owner:String(t.owner||'TBC'),commitDate:String(t.commitDate||'TBC'),stream:String(t.stream||''),dependency:String(t.dependency||'')}}
function cleanChange(c){return {type:String(c.type||''),task:String(c.task||''),detail:String(c.detail||'')}}
function fallbackReport(tasks,changes=[]){
 const overdue=tasks.filter(t=>/delay|overdue|late|blocked/i.test(t.status));
 const deteriorated=changes.filter(c=>c.type==="escalate");
 return `PMO RECOVERY SUMMARY

Current position: ${overdue.length} of ${tasks.length} analysed task(s) are flagged as delayed/overdue. ${changes.length} reporting-cycle change(s) were detected, including ${deteriorated.length} deterioration(s).

Required next actions:
1. Confirm root cause, corrective action and accountable PIC for each delayed task.
2. Validate cross-stream dependencies and blockers before committing the revised date.
3. Replace any TBC ETA with a realistic commitment approved by the responsible owner.
4. Escalate repeated missed commitments where the next milestone, test cycle or solution document is impacted.
5. Track whether the recovery plan is actually improving the position by comparing the next snapshot against the current baseline.`;
}
export default {
 async fetch(request, env){
  const url=new URL(request.url);
  if(url.pathname==="/" || url.pathname==="/index.html") return new Response(html,{headers:{"content-type":"text/html; charset=UTF-8"}});
  if(url.pathname==="/api/health") return Response.json({status:"online",service:"ProjectMind AI PMO Operating System",version:"3.0",aiConfigured:Boolean(env.OPENAI_API_KEY)},{headers:jsonHeaders});

  if(url.pathname==="/api/chat" && request.method==="POST"){
   let body;try{body=await request.json()}catch{return Response.json({error:"Invalid JSON"},{status:400,headers:jsonHeaders})}
   const message=String(body.message||"").trim();if(!message)return Response.json({error:"Message is required"},{status:400,headers:jsonHeaders});
   if(!env.OPENAI_API_KEY)return Response.json({reply:"ProjectMind V3 is active. Tracker analysis, local snapshots, change detection, alerts and executive daily brief work without an AI key. Add OPENAI_API_KEY in Cloudflare Variables and secrets to activate the full AI PMO advisor."},{headers:jsonHeaders});
   const system="You are ProjectMind AI, a senior global PMO, transformation and project delivery advisor. Be practical, structured and concise. Focus on issue, impact, root cause, next action, owner, ETA, dependency, recovery plan, commitment governance, change movement and escalation when relevant. Use project context directly. Do not invent facts not present in the tracker. For professional messages, write natural business English.";
   try{
    const model=env.OPENAI_MODEL||"gpt-4.1-mini";
    const context=Array.isArray(body.projectContext)?JSON.stringify(body.projectContext.slice(0,100)):"";
    const changeContext=Array.isArray(body.changeContext)?JSON.stringify(body.changeContext.slice(0,80)):"";
    const res=await fetch("https://api.openai.com/v1/chat/completions",{method:"POST",headers:{"Authorization":"Bearer "+env.OPENAI_API_KEY,"Content-Type":"application/json"},body:JSON.stringify({model,messages:[{role:"system",content:system},{role:"user",content:message+(context?"\\n\\nCurrent project tracker:\\n"+context:"")+(changeContext?"\\n\\nChanges since previous snapshot:\\n"+changeContext:"")}],temperature:0.35})});
    const data=await res.json();if(!res.ok)return Response.json({error:data.error?.message||"AI request failed"},{status:502,headers:jsonHeaders});
    return Response.json({reply:data.choices?.[0]?.message?.content||"No AI response returned."},{headers:jsonHeaders});
   }catch{return Response.json({error:"Unable to reach AI service."},{status:502,headers:jsonHeaders})}
  }

  if(url.pathname==="/api/analyze" && request.method==="POST"){
   let body;try{body=await request.json()}catch{return Response.json({error:"Invalid JSON"},{status:400,headers:jsonHeaders})}
   const tasks=Array.isArray(body.tasks)?body.tasks.slice(0,120).map(cleanTask):[];
   const changes=Array.isArray(body.changes)?body.changes.slice(0,80).map(cleanChange):[];
   if(!tasks.length)return Response.json({error:"No tasks supplied"},{status:400,headers:jsonHeaders});
   if(!env.OPENAI_API_KEY)return Response.json({report:fallbackReport(tasks,changes),mode:"local"},{headers:jsonHeaders});
   const prompt="Create an executive-ready PMO recovery analysis grounded only in the submitted tracker and reporting-cycle changes. Use sections: Executive Summary, Material Changes Since Previous Snapshot, Issue & Root Cause Gaps, Impact Analysis, Recovery Actions, Commitment Governance, Escalation Triggers. Explicitly identify deteriorations but do not invent causes.\\n\\nTasks:\\n"+JSON.stringify(tasks)+"\\n\\nChanges:\\n"+JSON.stringify(changes);
   try{
    const model=env.OPENAI_MODEL||"gpt-4.1-mini";
    const res=await fetch("https://api.openai.com/v1/chat/completions",{method:"POST",headers:{"Authorization":"Bearer "+env.OPENAI_API_KEY,"Content-Type":"application/json"},body:JSON.stringify({model,messages:[{role:"system",content:"You are a senior global PMO and recovery manager. Produce concise, decision-oriented analysis grounded only in the provided tracker and change data."},{role:"user",content:prompt}],temperature:0.25})});
    const data=await res.json();if(!res.ok)return Response.json({report:fallbackReport(tasks,changes),mode:"local"},{headers:jsonHeaders});
    return Response.json({report:data.choices?.[0]?.message?.content||fallbackReport(tasks,changes),mode:"ai"},{headers:jsonHeaders});
   }catch{return Response.json({report:fallbackReport(tasks,changes),mode:"local"},{headers:jsonHeaders})}
  }
  return new Response("Not found",{status:404});
 }
};