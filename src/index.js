const html = String.raw`<!doctype html>
<html lang="en"><head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<meta name="theme-color" content="#07111f"><title>ProjectMind AI Ultimate</title>
<script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js"></script>
<style>
:root{--bg:#07111f;--panel:#0d1a2b;--panel2:#11243b;--line:#223a56;--text:#eef5fc;--muted:#91a4ba;--blue:#5791ff;--green:#39d49a;--amber:#ffc454;--red:#ff6b80;--purple:#a38bff}
*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at top right,#12294a 0,#07111f 45%);color:var(--text);font-family:Inter,system-ui,-apple-system,Segoe UI,sans-serif}.app{max-width:1440px;margin:auto;padding:18px}.top{display:flex;justify-content:space-between;align-items:center;gap:14px}.brand{display:flex;gap:12px;align-items:center}.logo{width:46px;height:46px;border-radius:15px;background:linear-gradient(135deg,#5791ff,#8b6cff);display:grid;place-items:center;font-size:22px}.brand h1{margin:0;font-size:22px}.brand p{margin:3px 0;color:var(--muted);font-size:12px}.pill{padding:7px 10px;border-radius:99px;background:#102d26;color:var(--green);font-size:12px}.tabs{display:flex;gap:8px;overflow:auto;margin:18px 0}.tabs button,.btn{border:1px solid var(--line);background:var(--panel);color:var(--text);padding:10px 13px;border-radius:11px;cursor:pointer}.tabs button.active,.btn.primary{background:var(--blue);border-color:var(--blue)}.btn.good{background:#103429;color:var(--green)}.btn.warn{background:#3a2d12;color:var(--amber)}.btn.danger{background:#3a1821;color:var(--red)}.hero{background:linear-gradient(135deg,#0e1d31,#132b49);border:1px solid var(--line);border-radius:20px;padding:22px;margin-bottom:16px}.hero h2{margin:0 0 8px;font-size:26px}.hero p{margin:0;color:var(--muted);max-width:850px}.grid{display:grid;grid-template-columns:repeat(6,1fr);gap:12px}.card{background:linear-gradient(180deg,var(--panel),#0a1524);border:1px solid var(--line);border-radius:17px;padding:16px}.k{font-size:11px;color:var(--muted);text-transform:uppercase;letter-spacing:.05em}.v{font-size:30px;font-weight:800;margin-top:6px}.red{color:var(--red)}.green{color:var(--green)}.amber{color:var(--amber)}.blue{color:var(--blue)}.section{display:none}.section.active{display:block}.layout{display:grid;grid-template-columns:1.45fr .85fr;gap:14px}.panel{background:rgba(13,26,43,.92);border:1px solid var(--line);border-radius:18px;padding:17px}.panel h3{margin:0 0 12px;font-size:16px}.row{display:flex;gap:10px;flex-wrap:wrap;align-items:center}.grow{flex:1}.muted{color:var(--muted)}input,select,textarea{background:#081524;color:var(--text);border:1px solid var(--line);border-radius:10px;padding:10px;width:100%}textarea{min-height:180px;resize:vertical}.tablewrap{overflow:auto;max-height:520px;border:1px solid var(--line);border-radius:13px}table{border-collapse:collapse;width:100%;font-size:12px}th,td{padding:10px;border-bottom:1px solid #1a2d43;text-align:left;white-space:nowrap}th{position:sticky;top:0;background:#102039;color:#a9bdd3}.tag{padding:4px 8px;border-radius:99px;font-size:11px;display:inline-block}.tag.red{background:#3b1721}.tag.green{background:#103226}.tag.amber{background:#3c2d10}.tag.blue{background:#102d4b}.alert{padding:13px;border:1px solid var(--line);border-left:4px solid var(--amber);border-radius:12px;margin:8px 0;background:#0a1625}.alert.critical{border-left-color:var(--red)}.alert.good{border-left-color:var(--green)}.hidden{display:none}.drop{border:1.5px dashed #3a5b7d;border-radius:14px;padding:20px;text-align:center;color:var(--muted)}.mini{font-size:12px}.split{display:grid;grid-template-columns:1fr 1fr;gap:12px}.projectline{padding:11px;border:1px solid var(--line);border-radius:11px;margin:8px 0;cursor:pointer}.projectline.active{border-color:var(--blue);background:#0d2138}.badge{font-size:11px;padding:4px 7px;border-radius:8px;background:#132944;color:#9ec0ff}.footer{margin:18px 0 6px;color:#70849b;font-size:11px;text-align:center}
@media(max-width:1000px){.grid{grid-template-columns:repeat(3,1fr)}.layout,.split{grid-template-columns:1fr}}@media(max-width:640px){.app{padding:12px}.grid{grid-template-columns:repeat(2,1fr)}.v{font-size:25px}.hero h2{font-size:22px}.top{align-items:flex-start}.tabs button{white-space:nowrap}}
</style></head><body><div class="app">
<div class="top"><div class="brand"><div class="logo">🧠</div><div><h1>ProjectMind AI</h1><p>Ultimate PMO Operating System · Portfolio, Recovery & Executive Intelligence</p></div></div><div class="pill" id="mode">● Local Intelligence Active</div></div>
<div class="tabs" id="tabs">
<button class="active" data-tab="command">Command Center</button><button data-tab="portfolio">Portfolio</button><button data-tab="tracker">Tracker</button><button data-tab="changes">Change Intelligence</button><button data-tab="recovery">Recovery Room</button><button data-tab="executive">Executive Brief</button><button data-tab="advisor">AI Advisor</button><button data-tab="data">Data & Governance</button>
</div>

<section class="section active" id="command"><div class="hero"><h2>Project control, prioritised.</h2><p>One operating view for delivery health, deteriorating commitments, overdue actions, recovery governance and executive decision support.</p></div><div class="grid" id="kpis"></div><div class="layout" style="margin-top:14px"><div class="panel"><h3>PMO Alert Centre</h3><div id="alerts"></div></div><div class="panel"><h3>Today’s Control Actions</h3><div id="today"></div></div></div></section>

<section class="section" id="portfolio"><div class="layout"><div class="panel"><div class="row"><h3 class="grow">Project Portfolio</h3><button class="btn primary" id="newProject">+ New Project</button></div><div id="projectList"></div></div><div class="panel"><h3>Portfolio Health</h3><div id="portfolioHealth"></div><hr style="border-color:var(--line)"><div class="mini muted">ProjectMind Ultimate currently stores the portfolio locally in this browser. The data model is ready for a future shared cloud persistence layer without changing the operating workflow.</div></div></div></section>

<section class="section" id="tracker"><div class="panel"><div class="row"><h3 class="grow">Delivery Tracker</h3><select id="statusFilter" style="max-width:180px"><option value="">All status</option><option>Delayed</option><option>Overdue</option><option>At Risk</option><option>On Track</option><option>Completed</option></select><button class="btn" id="exportCsv">Export CSV</button></div><div class="tablewrap"><table><thead><tr><th>Task</th><th>Status</th><th>Stream</th><th>PIC</th><th>ETA / Commit</th><th>Dependency</th><th>PMO Action</th></tr></thead><tbody id="taskRows"></tbody></table></div></div></section>

<section class="section" id="changes"><div class="layout"><div class="panel"><h3>Reporting-cycle Change Intelligence</h3><div class="muted mini">Compares the current tracker against the most recent saved baseline for the selected project.</div><div id="changeList" style="margin-top:12px"></div></div><div class="panel"><h3>Snapshot Control</h3><div class="row"><button class="btn good" id="saveSnapshot">Save Current Snapshot</button><button class="btn danger" id="clearSnapshots">Clear Project Snapshots</button></div><p class="mini muted">Snapshots capture the tracker condition at a reporting point so status, ownership and commitment movement can be governed over time.</p><div id="snapshotInfo"></div></div></div></section>

<section class="section" id="recovery"><div class="layout"><div class="panel"><h3>Recovery Plan</h3><div id="recoveryPlan"></div></div><div class="panel"><h3>Recovery Governance Rules</h3><div class="alert critical"><b>No silent ETA movement.</b><br><span class="mini muted">Any revised commitment must retain the accountable PIC and a clear corrective action.</span></div><div class="alert"><b>Dependencies before dates.</b><br><span class="mini muted">A recovery commitment is not credible until upstream blockers are visible and owned.</span></div><div class="alert good"><b>Close the loop.</b><br><span class="mini muted">Final solutions should be reflected in the relevant solution documents and validated before the next control point.</span></div></div></div></section>

<section class="section" id="executive"><div class="split"><div class="panel"><div class="row"><h3 class="grow">Executive Daily Brief</h3><button class="btn primary" id="generateBrief">Generate</button><button class="btn" id="copyBrief">Copy</button></div><textarea id="brief" placeholder="Generate an executive-ready project brief."></textarea></div><div class="panel"><h3>Management Focus</h3><div id="managementFocus"></div></div></div></section>

<section class="section" id="advisor"><div class="split"><div class="panel"><h3>Ask ProjectMind AI</h3><textarea id="question" placeholder="Example: Which delayed items should be escalated today, and why?"></textarea><div class="row" style="margin-top:10px"><button class="btn primary" id="askAI">Analyse Portfolio / Project</button></div><textarea id="aiAnswer" placeholder="Analysis will appear here." style="margin-top:12px"></textarea></div><div class="panel"><h3>Advisor Scope</h3><div class="alert"><b>Recovery planning</b><br><span class="mini muted">Prioritise delayed commitments and identify immediate corrective actions.</span></div><div class="alert"><b>Escalation logic</b><br><span class="mini muted">Distinguish delivery noise from issues requiring management intervention.</span></div><div class="alert"><b>Executive communication</b><br><span class="mini muted">Convert tracker data into concise, decision-oriented management updates.</span></div></div></div></section>

<section class="section" id="data"><div class="layout"><div class="panel"><h3>Import Project Tracker</h3><div class="drop"><input type="file" id="file" accept=".xlsx,.xls,.csv"><p>Upload Excel or CSV. ProjectMind maps common fields such as Task, Status, PIC, Owner, ETA, Commit Date, Stream and Dependency.</p></div><div id="mapping" class="mini muted" style="margin-top:10px"></div></div><div class="panel"><h3>Data Quality & Governance</h3><div id="quality"></div><hr style="border-color:var(--line)"><div class="row"><button class="btn warn" id="demoData">Load Demo PMO Data</button><button class="btn danger" id="clearProject">Clear Current Project Data</button></div></div></div></section>
<div class="footer">ProjectMind AI Ultimate · PMO Operating System · Local-first foundation ready for continuous enhancement</div>
</div>
<script>
const K='projectmind-ultimate-v1';
let state=JSON.parse(localStorage.getItem(K)||'{"projects":[{"id":"default","name":"ProjectMind Demo","tasks":[]}],"active":"default","snapshots":{}}');
function save(){localStorage.setItem(K,JSON.stringify(state))}
function project(){return state.projects.find(function(p){return p.id===state.active})||state.projects[0]}
function esc(v){return String(v==null?'':v).replace(/[&<>"]/g,function(c){return ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c])})}
function field(o,n){const k=Object.keys(o).find(function(x){return x.toLowerCase().replace(/[^a-z0-9]/g,'')===n});return k?o[k]:''}
function normStatus(s){s=String(s||'').trim();if(/complete|done|closed/i.test(s))return'Completed';if(/delay|late|overdue/i.test(s))return/overdue/i.test(s)?'Overdue':'Delayed';if(/risk|block/i.test(s))return'At Risk';return s||'On Track'}
function clean(rows){return rows.map(function(r,i){let x={task:field(r,'task')||field(r,'activity')||field(r,'item')||('Task '+(i+1)),status:normStatus(field(r,'status')),stream:field(r,'stream')||field(r,'workstream')||'General',pic:field(r,'pic')||field(r,'owner')||field(r,'responsible')||'TBC',eta:field(r,'eta')||field(r,'commitdate')||field(r,'targetdate')||field(r,'duedate')||'TBC',dependency:field(r,'dependency')||field(r,'blocker')||'',action:field(r,'pmoaction')||field(r,'nextaction')||''};return x})}
function risk(t){return /delayed|overdue/i.test(t.status)?'red':/risk/i.test(t.status)?'amber':/complete/i.test(t.status)?'green':'blue'}
function metrics(){let t=project().tasks,total=t.length,over=t.filter(x=>/delayed|overdue/i.test(x.status)).length,ar=t.filter(x=>/risk/i.test(x.status)).length,done=t.filter(x=>/complete/i.test(x.status)).length,missing=t.filter(x=>!x.pic||x.pic==='TBC'||!x.eta||x.eta==='TBC').length,ch=changes();return{total:total,over:over,risk:ar,done:done,missing:missing,changed:ch.length,deter:ch.filter(x=>x.type==='DETERIORATED').length}}
function changes(){let p=project(), snaps=(state.snapshots[p.id]||[]);if(!snaps.length)return[];let old=snaps[snaps.length-1].tasks||[], map=new Map(old.map(x=>[x.task,x])), out=[];p.tasks.forEach(function(t){let o=map.get(t.task);if(!o){out.push({type:'NEW',task:t.task,detail:'New task added'})}else{let dif=[];if(o.status!==t.status)dif.push('Status: '+o.status+' → '+t.status);if(o.pic!==t.pic)dif.push('PIC: '+o.pic+' → '+t.pic);if(o.eta!==t.eta)dif.push('Commit: '+o.eta+' → '+t.eta);if(dif.length){let bad=(!/delayed|overdue|risk/i.test(o.status)&&/delayed|overdue/i.test(t.status))||(/complete/i.test(o.status)&&!/complete/i.test(t.status));out.push({type:bad?'DETERIORATED':'CHANGED',task:t.task,detail:dif.join(' · ')})}}});old.forEach(function(o){if(!p.tasks.some(x=>x.task===o.task))out.push({type:'REMOVED',task:o.task,detail:'Task no longer present in current tracker'})});return out}
function render(){let m=metrics(), p=project();document.getElementById('kpis').innerHTML=[['Total',m.total,'blue'],['Overdue',m.over,'red'],['At Risk',m.risk,'amber'],['Completed',m.done,'green'],['Changed',m.changed,'blue'],['Deteriorated',m.deter,'red']].map(x=>'<div class="card"><div class="k">'+x[0]+'</div><div class="v '+x[2]+'">'+x[1]+'</div></div>').join('');renderAlerts(m);renderToday();renderProjects();renderTasks();renderChanges();renderRecovery();renderQuality();renderFocus();renderPortfolio()}
function renderAlerts(m){let a=[];if(!m.total)a.push(['critical','No delivery tracker loaded','Import the current project tracker to start PMO control.']);if(m.over)a.push(['critical',m.over+' delayed / overdue commitment(s)','Confirm root cause, accountable PIC, corrective action and realistic ETA.']);if(m.deter)a.push(['critical',m.deter+' item(s) deteriorated since baseline','Escalate movement that affects the next milestone, test cycle or delivery commitment.']);if(m.risk)a.push(['',m.risk+' at-risk item(s)','Monitor dependencies and confirm whether risk mitigation is time-bound and owned.']);if(m.missing)a.push(['',m.missing+' governance gap(s)','PIC or ETA is missing / TBC and should not remain open without a control action.']);if(!a.length)a.push(['good','Portfolio is currently stable','No delayed or at-risk task is detected in the loaded data.']);document.getElementById('alerts').innerHTML=a.map(x=>'<div class="alert '+x[0]+'"><b>'+esc(x[1])+'</b><br><span class="mini muted">'+esc(x[2])+'</span></div>').join('')}
function renderToday(){let t=project().tasks.filter(x=>/delayed|overdue|risk/i.test(x.status)).slice(0,8);document.getElementById('today').innerHTML=t.length?t.map(x=>'<div class="projectline"><b>'+esc(x.task)+'</b><div class="mini muted">'+esc(x.pic)+' · '+esc(x.eta)+' · '+esc(x.status)+'</div></div>').join(''):'<div class="muted mini">No immediate recovery item detected.</div>'}
function renderProjects(){document.getElementById('projectList').innerHTML=state.projects.map(function(p){let mm=(p.tasks||[]).filter(x=>/delayed|overdue/i.test(x.status)).length;return '<div class="projectline '+(p.id===state.active?'active':'')+'" data-p="'+p.id+'"><b>'+esc(p.name)+'</b><div class="mini muted">'+(p.tasks||[]).length+' tasks · '+mm+' delayed/overdue</div></div>'}).join('');document.querySelectorAll('[data-p]').forEach(function(e){e.onclick=function(){state.active=e.dataset.p;save();render()}})}
function renderTasks(){let q=document.getElementById('statusFilter').value.toLowerCase(),t=project().tasks.filter(x=>!q||x.status.toLowerCase()===q);document.getElementById('taskRows').innerHTML=t.map(x=>'<tr><td><b>'+esc(x.task)+'</b></td><td><span class="tag '+risk(x)+'">'+esc(x.status)+'</span></td><td>'+esc(x.stream)+'</td><td>'+esc(x.pic)+'</td><td>'+esc(x.eta)+'</td><td>'+esc(x.dependency||'—')+'</td><td>'+esc(x.action||'—')+'</td></tr>').join('')||'<tr><td colspan="7" class="muted">No tasks available.</td></tr>'}
function renderChanges(){let c=changes(),s=(state.snapshots[project().id]||[]);document.getElementById('changeList').innerHTML=c.length?c.map(x=>'<div class="alert '+(x.type==='DETERIORATED'?'critical':'')+'"><b>'+esc(x.type)+' · '+esc(x.task)+'</b><br><span class="mini muted">'+esc(x.detail)+'</span></div>').join(''):'<div class="muted mini">No comparison available yet. Save a snapshot, then load an updated tracker.</div>';document.getElementById('snapshotInfo').innerHTML=s.length?'<div class="mini muted">'+s.length+' snapshot(s). Latest: '+new Date(s[s.length-1].at).toLocaleString()+'</div>':'<div class="mini muted">No snapshot saved for this project.</div>'}
function renderRecovery(){let t=project().tasks.filter(x=>/delayed|overdue|risk/i.test(x.status));document.getElementById('recoveryPlan').innerHTML=t.length?t.map(function(x,i){let a=x.action||'Confirm root cause, corrective action, accountable PIC and approved recovery ETA.';return '<div class="projectline"><span class="badge">P'+(i+1)+'</span> <b>'+esc(x.task)+'</b><div class="mini muted" style="margin-top:6px">Owner: '+esc(x.pic)+' · Current commitment: '+esc(x.eta)+'</div><div class="mini" style="margin-top:7px">'+esc(a)+'</div></div>'}).join(''):'<div class="muted mini">No recovery action generated because no delayed or at-risk task is loaded.</div>'}
function renderQuality(){let m=metrics(),score=m.total?Math.max(0,100-Math.round((m.missing/m.total)*100)):0;document.getElementById('quality').innerHTML='<div class="v '+(score<70?'red':score<90?'amber':'green')+'">'+score+'%</div><div class="mini muted">Tracker governance completeness based on missing PIC / ETA fields.</div><div class="alert"><b>Recommended control:</b><br><span class="mini muted">Replace TBC commitments before the next PMO control point and maintain a single accountable owner per action.</span></div>'}
function renderFocus(){let m=metrics();let x=[];if(m.deter)x.push('Stop deterioration: '+m.deter+' item(s) worsened versus baseline.');if(m.over)x.push('Recover '+m.over+' delayed / overdue commitment(s).');if(m.missing)x.push('Close '+m.missing+' PIC or ETA governance gap(s).');if(!x.length)x.push('Maintain current commitments and validate upcoming dependencies.');document.getElementById('managementFocus').innerHTML=x.map(v=>'<div class="alert">'+esc(v)+'</div>').join('')}
function renderPortfolio(){let total=state.projects.length,tasks=state.projects.reduce((a,p)=>a+(p.tasks||[]).length,0),bad=state.projects.reduce((a,p)=>a+(p.tasks||[]).filter(x=>/delayed|overdue/i.test(x.status)).length,0);document.getElementById('portfolioHealth').innerHTML='<div class="v blue">'+total+'</div><div class="mini muted">Active project(s)</div><div class="v" style="margin-top:16px">'+tasks+'</div><div class="mini muted">Portfolio tasks</div><div class="v '+(bad?'red':'green')+'" style="margin-top:16px">'+bad+'</div><div class="mini muted">Delayed / overdue across portfolio</div>'}
document.getElementById('tabs').onclick=function(e){if(e.target.tagName!=='BUTTON')return;document.querySelectorAll('.tabs button').forEach(x=>x.classList.remove('active'));e.target.classList.add('active');document.querySelectorAll('.section').forEach(x=>x.classList.remove('active'));document.getElementById(e.target.dataset.tab).classList.add('active')}
document.getElementById('statusFilter').onchange=renderTasks;
document.getElementById('newProject').onclick=function(){let n=prompt('Project name');if(!n)return;let id='p'+Date.now();state.projects.push({id:id,name:n,tasks:[]});state.active=id;save();render()}
document.getElementById('file').onchange=function(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=function(ev){let data=new Uint8Array(ev.target.result),wb=XLSX.read(data,{type:'array'}),sheet=wb.Sheets[wb.SheetNames[0]],rows=XLSX.utils.sheet_to_json(sheet,{defval:''});project().tasks=clean(rows);save();document.getElementById('mapping').textContent='Imported '+rows.length+' row(s) from '+f.name+'.';render()};r.readAsArrayBuffer(f)}
document.getElementById('saveSnapshot').onclick=function(){let p=project();state.snapshots[p.id]=state.snapshots[p.id]||[];state.snapshots[p.id].push({at:Date.now(),tasks:JSON.parse(JSON.stringify(p.tasks))});save();render()}
document.getElementById('clearSnapshots').onclick=function(){if(confirm('Clear all snapshots for this project?')){state.snapshots[project().id]=[];save();render()}}
document.getElementById('clearProject').onclick=function(){if(confirm('Clear current project tracker?')){project().tasks=[];save();render()}}
document.getElementById('demoData').onclick=function(){project().tasks=clean([{Task:'SIT defect resolution',Status:'Delayed',Stream:'Testing',PIC:'Vendor SIT Lead',ETA:'12 Sep',Dependency:'Environment stability',NextAction:'Daily defect burn-down and approved recovery plan'},{Task:'Payment integration validation',Status:'At Risk',Stream:'Integration',PIC:'Integration Lead',ETA:'10 Sep',Dependency:'External API',NextAction:'Confirm client availability and fallback test window'},{Task:'Solution document update',Status:'On Track',Stream:'Architecture',PIC:'Solution Architect',ETA:'09 Sep',Dependency:'Final SIT findings',NextAction:'Reflect final agreed solution before PB2'},{Task:'Environment readiness',Status:'Completed',Stream:'Platform',PIC:'Infra Lead',ETA:'Completed',Dependency:'',NextAction:''}]);save();render()}
document.getElementById('exportCsv').onclick=function(){let rows=[['Task','Status','Stream','PIC','ETA','Dependency','PMO Action']].concat(project().tasks.map(x=>[x.task,x.status,x.stream,x.pic,x.eta,x.dependency,x.action]));let csv=rows.map(r=>r.map(v=>'"'+String(v).replace(/"/g,'""')+'"').join(',')).join('\n'),a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='ProjectMind_Ultimate_'+project().name.replace(/\W+/g,'_')+'.csv';a.click()}
function brief(){let p=project(),m=metrics(),c=changes(),lines=[];lines.push('PROJECTMIND EXECUTIVE DAILY BRIEF');lines.push('Project: '+p.name);lines.push('');lines.push('Current position: '+m.over+' delayed/overdue, '+m.risk+' at risk, '+m.done+' completed out of '+m.total+' tracked item(s).');if(m.deter)lines.push('Trend: '+m.deter+' item(s) deteriorated since the previous baseline.');if(m.missing)lines.push('Governance: '+m.missing+' item(s) still have a missing or TBC PIC/ETA.');lines.push('');lines.push('Management attention:');p.tasks.filter(x=>/delayed|overdue|risk/i.test(x.status)).slice(0,5).forEach(x=>lines.push('- '+x.task+' | '+x.status+' | PIC: '+x.pic+' | Commitment: '+x.eta));lines.push('');lines.push('Required actions today:');lines.push('1. Confirm root cause and corrective action for each delayed commitment.');lines.push('2. Validate dependencies before accepting any revised ETA.');lines.push('3. Escalate repeated deterioration affecting the next milestone or test cycle.');lines.push('4. Ensure final solutions are reflected in all related solution documents.');return lines.join('\n')}
document.getElementById('generateBrief').onclick=function(){document.getElementById('brief').value=brief()};document.getElementById('copyBrief').onclick=function(){navigator.clipboard.writeText(document.getElementById('brief').value)}
document.getElementById('askAI').onclick=async function(){let q=document.getElementById('question').value||'Provide a concise PMO recovery assessment and escalation priorities.';let ans=document.getElementById('aiAnswer');ans.value='Analysing project data...';try{let r=await fetch('/api/analyze',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question:q,tasks:project().tasks,changes:changes()})}),d=await r.json();ans.value=d.report||'No analysis returned.';document.getElementById('mode').textContent=d.mode==='ai'?'● AI Advisor Connected':'● Local Intelligence Active'}catch(e){ans.value='Unable to reach the analysis service. The local PMO workflow remains available.'}}
render();
</script></body></html>`;

function fallbackReport(tasks,changes,question){
 const bad=tasks.filter(function(t){return /delay|overdue|late|blocked/i.test(t.status)});
 const risk=tasks.filter(function(t){return /risk/i.test(t.status)});
 const det=changes.filter(function(c){return c.type==='DETERIORATED'});
 return 'PMO ANALYSIS\n\nQuestion: '+question+'\n\nCurrent position: '+bad.length+' delayed/overdue and '+risk.length+' at-risk item(s) out of '+tasks.length+' tracked task(s). '+changes.length+' reporting-cycle movement(s) detected, including '+det.length+' deterioration(s).\n\nPriority actions:\n1. For each delayed item, confirm root cause, corrective action, accountable PIC and realistic recovery ETA.\n2. Validate dependencies before accepting revised commitments.\n3. Escalate repeated deterioration where the next milestone, SIT/UAT cycle or delivery commitment is impacted.\n4. Close TBC ownership and ETA gaps before the next PMO control point.\n5. Reflect final agreed solutions in all related solution documents before the relevant playback or approval gate.';
}

export default {
 async fetch(request,env){
  const url=new URL(request.url);
  if(url.pathname==='/health')return Response.json({status:'online',service:'ProjectMind AI Ultimate',version:'4.0'});
  if(url.pathname==='/api/analyze' && request.method==='POST'){
   const body=await request.json();const tasks=Array.isArray(body.tasks)?body.tasks:[];const changes=Array.isArray(body.changes)?body.changes:[];const question=String(body.question||'Provide PMO analysis');
   const local=fallbackReport(tasks,changes,question);
   if(!env.OPENAI_API_KEY)return Response.json({report:local,mode:'local'});
   try{
    const model=env.OPENAI_MODEL||'gpt-4.1-mini';
    const prompt='Question: '+question+'\n\nTracker:\n'+JSON.stringify(tasks)+'\n\nChanges:\n'+JSON.stringify(changes);
    const res=await fetch('https://api.openai.com/v1/chat/completions',{method:'POST',headers:{Authorization:'Bearer '+env.OPENAI_API_KEY,'Content-Type':'application/json'},body:JSON.stringify({model:model,messages:[{role:'system',content:'You are a senior global PMO and recovery manager. Give concise, decision-oriented analysis grounded only in the supplied tracker and change data. Do not invent facts.'},{role:'user',content:prompt}],temperature:0.2})});
    const data=await res.json();if(!res.ok)return Response.json({report:local,mode:'local'});
    return Response.json({report:data.choices&&data.choices[0]&&data.choices[0].message&&data.choices[0].message.content||local,mode:'ai'});
   }catch(e){return Response.json({report:local,mode:'local'})}
  }
  return new Response(html,{headers:{'content-type':'text/html;charset=UTF-8','cache-control':'no-store'}});
 }
};